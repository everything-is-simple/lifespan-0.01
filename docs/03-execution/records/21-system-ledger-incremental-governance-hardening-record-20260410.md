# 全系统历史账本增量治理硬约束记录

记录编号：`21`
日期：`2026-04-10`

## 做了什么

1. 开卡 `21`，把卡 `17/19/20` 已验证的历史账本机制提升为全系统治理目标。
2. 重写共享合同，明确：
   - 稳定实体锚点优先
   - `name` 不是正式主锚
   - `run_id` 只做审计
   - 所有正式实现都必须声明批量建仓、增量更新、断点续跑与审计账本
3. 重写执行卡模板，新增 `## 历史账本约束` 六条声明。
4. 重写 `check_doc_first_gating_governance.py`，把该段纳入正式硬门禁。
5. 新增并通过 `tests/unit/system/test_doc_first_gating_governance.py`。
6. 同步入口文件与执行索引，并把最新生效结论锚点切换到卡 `21`。

## 偏离项

- 没有在卡 `21` 重做卡 `20` 的 `index/block raw->base` 初始化；该能力已经在卡 `20` 完成并验证，本卡只把其机制提升为全系统硬约束。

## 备注

- 经过卡 `21` 后，后续任何正式卡如果不显式说明“自然键是什么、怎么建仓、怎么增量、怎么续跑、审计账本在哪”，都不能合法进入 `src/`、`scripts/`、`.codex/` 的正式实现。
