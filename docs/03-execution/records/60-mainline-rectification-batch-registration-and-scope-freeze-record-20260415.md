# mainline rectification batch registration and scope freeze 记录
`记录编号`：`60`
`日期`：`2026-04-15`

## 执行过程概述

本卡不修改正式业务代码，只处理主线施工顺序和执行索引的正式口径。执行动作分为三步：

1. 新注册 `60 -> 66` 整改卡组，并把原 `60 -> 66` middle-ledger 分段建库卡整体后移为 `78 -> 84`。
2. 回填仓库入口文件、系统级路线图和执行索引，使“当前待施工卡”从 `59` 收口后正式切到 `60`。
3. 明确 `100 -> 105` 仍然只能位于 `95` 之后，禁止因为 `59` 通过而提前恢复 trade/system。

## 关键记录

1. 本卡收口后，`60-66` 成为 `59` 之后唯一 active 的主线整改卡组。
2. `78-84` 被保留而非废弃，但其身份从“默认续推主线”改为“整改收口后的恢复卡组”。
3. `59` 的作用被限定为 pilot truthfulness gate，不再单独承担“后续直接续推”依据。

## 变更清单

| 类型 | 路径 | 说明 |
| --- | --- | --- |
| 新增 card | `docs/03-execution/60-mainline-rectification-batch-registration-and-scope-freeze-card-20260415.md` | 整改批次登记卡 |
| 新增 card | `docs/03-execution/61-66-*.md` | 主线整改卡组 |
| 更新入口 | `README.md / AGENTS.md` | 当前锚点与当前待施工卡同步 |
| 更新路线图 | `docs/02-spec/Ω-system-delivery-roadmap-20260409.md` | 主线顺序改为 `60-66 -> 78-84 -> 100-105` |
| 更新索引 | `docs/03-execution/00 / A / B / C` | 当前锚点与执行顺序同步 |

## 收口判断

`60` 的完成标准不是“问题已经修完”，而是“问题已经被正式登记成主线整改批次，且后续施工顺序不再错位”。本卡完成后，主线可围绕 `61 -> 66` 逐项收口。

